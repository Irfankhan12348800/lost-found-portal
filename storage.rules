rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Sirf login kiya hua user read/write kar sake
      allow read, write: if request.auth != null;
    }
  }
}
